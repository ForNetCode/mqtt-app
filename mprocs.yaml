procs:
  #mqtt:
  #  shell: cd develop && ./run_mqtt.sh
  mproxy:
    env:
      RUST_LOG: debug
    shell: cd agent && cargo run -- $(pwd)/config.yml
  web:
    shell: cd web && npm run dev
    stop: SIGKILL